import 'package:surveycat_app/helpers/databaseProvider.dart';
import 'package:surveycat_app/models/parcela.dart';

class ClientProvider {
  /*  final dbProvider = DatabaseProvider.dbProvider;

  Future<int> createParcela(Parcela parcela) async {
    final db = await dbProvider.database;
    var result = db.insert(parcelaTABLE, parcela.toJson());
    return result; */
  //}
}
